# Crown volume representation pilot

The roof material pilot failed whole-image review and was restored to XXVIII. Reference has coherent irregular crown masses; current leaf representation is porous. Test a hierarchical 3D canopy at the same 46 tree frames, trunks, existing heights and locations. Do not add opaque ellipsoid blobs or flat tree billboards. All surfaces remain true leaves using the existing CC0 leaf texture.

Baseline main visible expanded vertices 7,526,397, leaf vertices 1,472,360. Existing each tree16,000 near /8,000 far. Final near retains all16000 authored branch-tip leaves and adds6000 fine volumetric fill leaves; far retains2000 authored and3000 fill using existing far geometry with area compensation. Near geometry increases112000→154000 vertices; far reduces32000→20000. Main/overview all46 crowns use far: expanded vertices7,526,397→6,974,397. Near views can cost more per nearby tree; guards remain unchanged. Leaf fill distributed among deterministic source crown clusters, preserving source envelope and tree frames. Cluster geometry is actual folded leaves, never an opaque sphere. Full-source spatial clusters computed once. Both LODs share base fill scale .55: leaf length .165–.242m and width ratio .45–.68; far uses only geometry/retention compensation, source spatial shape transformed per tree; these are artistic broad-leaf approximations, not scanned species claims.

Fit9 deterministic k-means clusters to source placements,10 iterations sampled every4; bounded per-axis radii from cluster variance with .08 minimum. Sample within ellipsoids, clamp to source min/max. 6000 fill placements shared, orientations varied deterministically with mild upward bias. Root calculates transformed positions and leaf frames. Use same near/far material and existing LOD38 threshold. Preserve silhouette tip leaves with stratified authored sampling. No shader-only occlusion/lighting baked into geometry colors. Mean leaf shade near old range; real lighting and shadows determine volumes.

Test deterministic finite clustered output, original source not mutated, positions within bounds, fixed budget and existing tree matrices intact. Compare main, overview, rotated, close foliage view against28; retain only with visible volume gain and no artificial clump/oversized leaf artifact. Path budget guard unchanged; no commits/deploy. Full tests/128 path if retained. If rejected archive and restore28.

Final review: initial far-only visual benefit had oversized near leaves; restored all authored near leaves and fine fill. A3.31x fill area mismatch was detected and corrected by sharing .55 base scale. Regression compares aggregate near/far fill planform area. Balanced screenshot review retains modest visible gain; identical-camera38.10m LOD detail change remains but no large density/silhouette jump.
